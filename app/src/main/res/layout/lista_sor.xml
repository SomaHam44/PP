<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="16dp"
    android:paddingTop="10dp"
    android:paddingRight="16dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/IDTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="1" />

    <TextView
        android:id="@+id/gyogyszerNevTextView"
        android:layout_width="262dp"
        android:layout_height="25dp"
        android:layout_marginStart="16dp"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@+id/IDTextView"
        app:layout_constraintStart_toEndOf="@+id/IDTextView"
        app:layout_constraintTop_toTopOf="@+id/IDTextView"
        tools:text="Gyógyszer neve" />

    <TextView
        android:id="@+id/keszletTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        app:layout_constraintBottom_toBottomOf="@+id/gyogyszerNevTextView"
        app:layout_constraintStart_toEndOf="@+id/gyogyszerNevTextView"
        app:layout_constraintTop_toTopOf="@+id/gyogyszerNevTextView"
        tools:text="0" />

    <TextView
        android:id="@+id/DarabTextView"
        android:layout_width="22dp"
        android:layout_height="19dp"
        android:layout_marginStart="8dp"
        app:layout_constraintBottom_toBottomOf="@+id/KeszletTextView"
        app:layout_constraintStart_toEndOf="@+id/KeszletTextView"
        app:layout_constraintTop_toTopOf="@+id/KeszletTextView"
        tools:text="db" />


</androidx.constraintlayout.widget.ConstraintLayout>